apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fastapi-observability.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "fastapi-observability.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "fastapi-observability.fullname" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
