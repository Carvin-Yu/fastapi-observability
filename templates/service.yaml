apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-observability.fullname" . }}
spec:
  selector:
    app: {{ include "fastapi-observability.fullname" . }}
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 33334
  type: NodePort
